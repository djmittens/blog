version: 2
jobs:
  build-job:
    machine: true
    # docker:
    #   - image: debian:stretch
    steps:
      # Reference a "R/W" deploy key
      - add_ssh_keys:
          fingerprints:
            - "7a:5a:6b:be:98:1d:e0:3c:2d:cb:f1:11:bf:5b:df:24"
      - checkout
      - run:
          name: Docker build
          command: ./bin/build.sh
  deploy-job:
    machine: true
    # docker:
    #   - image: debian:stretch
    steps:
      # Reference a "R/W" deploy key
      - add_ssh_keys:
          fingerprints:
            - "7a:5a:6b:be:98:1d:e0:3c:2d:cb:f1:11:bf:5b:df:24"
      - checkout
      - run:
          name: Publish to GH Pages
          command: ./bin/publish.sh

workflows: 
  version: 2
  build-deploy:
    jobs:
      - build-job
      - deploy-job:
          requires:
            - build-job
          filters:
            branches:
              only: master
